<!DOCTYPE html>
<html>
<head>
    <title>Looper {{if not .Config.ReleaseMode}} (dev){{end}}</title>
    <link rel="stylesheet" href="/static/css/tui.css?t={{.Config.DebugCacheBust}}" />
    <link rel="stylesheet" href="/static/css/index.css?t={{.Config.DebugCacheBust}}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" /> <!-- Adaptive phone design -->
    <link rel="icon" type="image/png" href="/static/favicon{{if not .Config.ReleaseMode}}-debug{{end}}.png">
</head>
<body>
    <h1>The Looper</h1>
    <div id="tags"></div>
    <br />

    <div id="tasks">
        <span class="punctuation">Loading...</span>
    </div>
    <br />

    <textarea rows="1" id="input" enterkeyhint="enter" placeholder="{{.Idiom}}" autofocus></textarea>

    <script src="/static/lib/axios.min.js"></script>
    <script type="module" src="/static/js/index.js?t={{.Config.DebugCacheBust}}"></script>
</body>
</html>

