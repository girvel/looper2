<!DOCTYPE html>
<html>
<head>
    <title>Looper{{if not .ReleaseMode}} (dev){{end}}</title>
    <link rel="stylesheet" href="{{.StaticRoot}}/css/index.css" />
    {{ template "head" . }}
</head>
<body>
    <h1><a href="/" class="suppress_link_decoration">The Looper</a></h1>
    <div id="tags"></div>
    <br />

    <div id="remainder" class="punctuation"></div>
    <div id="tasks" class="scroll_shadow">
        <span class="punctuation">Loading...</span>
    </div>

    <div class="input_wrapper">
        <textarea
            rows="1"
            id="input"
            enterkeyhint="enter"
            placeholder="{{.Idiom}}"
            autofocus
        ></textarea>
        <button id="input_clear"> ⌫ </button>
    </div>
    <br />
    <span id="status"></span>

    <script type="module" src="{{.StaticRoot}}/js/index.js"></script>
</body>
</html>

